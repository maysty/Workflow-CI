name: HeartDisease-MLflow-Project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset_path:
        type: str
        default: heart_preprocessing/heart_clean.csv
    command: "python modelling.py --dataset_path {dataset_path}"